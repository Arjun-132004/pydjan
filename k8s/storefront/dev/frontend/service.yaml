apiVersion: v1
kind: Service
metadata:
  name: pydjan-frontend
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: pydjan-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pydjan-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
