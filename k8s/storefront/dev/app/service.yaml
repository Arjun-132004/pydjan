apiVersion: v1
kind: Service
metadata:
  name: pydjan-app
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: pydjan-app
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pydjan-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
